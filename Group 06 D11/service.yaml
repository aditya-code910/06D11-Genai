apiVersion: v1
kind: Service
metadata:
  name: manufacturing-service
spec:
  type: NodePort
  selector:
    app: manufacturing
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007
